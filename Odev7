1)SELECT rating FROM film
GROUP BY rating;

2)SELECT COUNT(*), replacement_cost FROM film
GROUP BY replacement_cost
HAVING COUNT(*) > 50;

3)SELECT COUNT(customer_id), store_id FROM customer
GROUP BY store_id;

4) SELECT COUNT(city), country_id FROM city
GROUP BY country_id;
